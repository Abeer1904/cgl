// js/ip.js
// No JS required for ip.html right now.
// Keeping this file so the page can grow later (filters, toggles, submissions) without changing structure.

(() => {
  "use strict";
  // Placeholder: add interactions here when needed.
})();